# Copy this file to .env and fill in your values.
# .env is gitignored — never commit credentials.

# --method google
# Path to the client_secret.json downloaded from Google Cloud Console
GOOGLE_CREDENTIALS_FILE=client_secret.json

# --method graph
# Application (client) ID from your Azure App Registration
GRAPH_CLIENT_ID=your-azure-app-client-id-here
